import { KeyToStringPipe } from './key-to-string.pipe';

describe('KeyToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
